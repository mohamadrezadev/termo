# 🚀 راهنمای راه‌اندازی سرور Thermal Analyzer

این راهنما به شما کمک می‌کند تا سرور تحلیل حرارتی را به صورت گام‌به‌گام راه‌اندازی کنید.

---

## 📋 پیش‌نیازها

### 1. نصب Python
- **نسخه مورد نیاز**: Python 3.8 یا بالاتر
- **دانلود**: از [python.org](https://www.python.org/downloads/) دانلود کنید
- **نکته مهم**: حتماً گزینه "Add Python to PATH" را در زمان نصب فعال کنید

### 2. بررسی نصب Python
در PowerShell یا CMD دستور زیر را اجرا کنید:
```bash
python --version
```
خروجی باید نسخه Python را نمایش دهد، مثلاً: `Python 3.11.0`

### 3. نصب Visual Studio (اختیاری - برای C# Application)
اگر قصد دارید از قابلیت استخراج BMT استفاده کنید:
- Visual Studio 2019 یا بالاتر
- یا حداقل .NET Framework 4.7.2

---

## 🔧 روش اول: نصب خودکار (پیشنهادی)

### گام 1: باز کردن پوشه سرور
1. به پوشه `server` در پروژه بروید
2. آدرس کامل: `D:\پروژه های دانش بنیان\termo2\termo\server`

### گام 2: اجرای فایل نصب خودکار
**روش ساده (دوبار کلیک):**
- روی فایل `start-server.bat` دوبار کلیک کنید

**یا در PowerShell:**
```powershell
.\start-server.bat
```

### گام 3: منتظر بمانید
فایل `start-server.bat` به صورت خودکار:
- ✅ Python را بررسی می‌کند
- ✅ محیط مجازی (virtual environment) ایجاد می‌کند
- ✅ وابستگی‌ها را نصب می‌کند
- ✅ پوشه‌های مورد نیاز را می‌سازد
- ✅ سرور را راه‌اندازی می‌کند

### گام 4: تایید راه‌اندازی
پس از اتمام نصب، پیام زیر را مشاهده خواهید کرد:
```
╔════════════════════════════════════════════════════════════╗
║  سرور در حال اجرا است:                                    ║
║  آدرس: http://127.0.0.1:8000                              ║
║  مستندات API: http://127.0.0.1:8000/docs                 ║
╚════════════════════════════════════════════════════════════╝
```

---

## 🛠️ روش دوم: نصب دستی قدم‌به‌قدم

### گام 1: ایجاد محیط مجازی Python
```powershell
cd server
python -m venv venv
```

### گام 2: فعال‌سازی محیط مجازی
**در Windows PowerShell:**
```powershell
.\venv\Scripts\Activate.ps1
```

**در Windows CMD:**
```cmd
venv\Scripts\activate.bat
```

**نشانه موفقیت:**
خط فرمان شما باید با `(venv)` شروع شود:
```
(venv) PS D:\...\server>
```

### گام 3: به‌روزرسانی pip
```powershell
python -m pip install --upgrade pip
```

### گام 4: نصب وابستگی‌ها (آنلاین)
```powershell
pip install -r requirements.txt
```

### گام 5: ایجاد پوشه‌های مورد نیاز
```powershell
mkdir temp_uploads -Force
mkdir extracted_images -Force
mkdir projects -Force
mkdir data -Force
```

### گام 6: اجرای سرور
```powershell
python run.py
```

یا:
```powershell
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

---

## 📦 نصب آفلاین (بدون اینترنت)

اگر دسترسی به اینترنت ندارید، می‌توانید وابستگی‌ها را از قبل دانلود و نصب کنید.

### مرحله 1: دانلود وابستگی‌ها (روی سیستم متصل به اینترنت)

#### 1.1 ایجاد پوشه wheelhouse
```powershell
cd server
mkdir wheelhouse
```

#### 1.2 دانلود همه وابستگی‌ها
```powershell
pip download -r requirements.txt -d wheelhouse
```

این دستور همه فایل‌های `.whl` و `.tar.gz` مورد نیاز را در پوشه `wheelhouse` ذخیره می‌کند.

### مرحله 2: انتقال فایل‌ها
1. پوشه `wheelhouse` را به همراه پروژه روی سیستم آفلاین کپی کنید
2. مطمئن شوید که ساختار پوشه‌ها حفظ شده است:
```
server/
├── wheelhouse/
│   ├── fastapi-0.104.1-py3-none-any.whl
│   ├── uvicorn-0.24.0-py3-none-any.whl
│   ├── ...
├── requirements.txt
└── run.py
```

### مرحله 3: نصب آفلاین

#### 3.1 ایجاد و فعال‌سازی محیط مجازی
```powershell
cd server
python -m venv venv
.\venv\Scripts\Activate.ps1
```

#### 3.2 نصب از wheelhouse
```powershell
pip install --no-index --find-links=wheelhouse -r requirements.txt
```

**توضیح پارامترها:**
- `--no-index`: از PyPI استفاده نکن (آنلاین نرو)
- `--find-links=wheelhouse`: از این پوشه فایل‌ها را بردار
- `-r requirements.txt`: لیست پکیج‌های مورد نیاز

#### 3.3 اجرای سرور
```powershell
python run.py
```

---

## 🔍 بررسی وضعیت نصب

### چک کردن نسخه Python
```powershell
python --version
```

### چک کردن نسخه pip
```powershell
pip --version
```

### لیست پکیج‌های نصب شده
```powershell
pip list
```

### بررسی وابستگی‌های مورد نیاز
```powershell
pip check
```

---

## 📂 ساختار پوشه‌ها

پس از راه‌اندازی موفق، ساختار زیر ایجاد می‌شود:

```
server/
├── app/                      # کد اصلی برنامه
│   ├── main.py              # نقطه ورود FastAPI
│   ├── api/                 # API endpoints
│   ├── models/              # مدل‌های دیتابیس
│   ├── services/            # منطق کسب‌وکار
│   └── core/                # تنظیمات و کانفیگ
├── venv/                     # محیط مجازی Python
├── wheelhouse/               # وابستگی‌های آفلاین (اختیاری)
├── temp_uploads/             # فایل‌های آپلود موقت
├── extracted_images/         # تصاویر استخراج شده
├── projects/                 # پروژه‌های ذخیره شده
├── data/                     # داده‌های برنامه
├── requirements.txt          # لیست وابستگی‌ها
├── run.py                   # فایل اجرای ساده
└── start-server.bat         # اسکریپت راه‌اندازی خودکار
```

---

## 🌐 دسترسی به سرور

پس از راه‌اندازی موفق، می‌توانید از آدرس‌های زیر استفاده کنید:

### 1. صفحه اصلی
```
http://127.0.0.1:8000
```

### 2. مستندات تعاملی API (Swagger UI)
```
http://127.0.0.1:8000/docs
```
- امکان تست تمام APIها
- مشاهده پارامترها و خروجی‌ها
- اجرای مستقیم درخواست‌ها

### 3. مستندات API (ReDoc)
```
http://127.0.0.1:8000/redoc
```
- مستندات کامل و حرفه‌ای
- قابل چاپ و ذخیره

### 4. OpenAPI Schema
```
http://127.0.0.1:8000/api/v1/openapi.json
```

---

## 🔧 تنظیمات پیشرفته

### تغییر پورت سرور
در فایل `run.py` خط زیر را ویرایش کنید:
```python
uvicorn.run(
    "app.main:app",
    host="0.0.0.0",
    port=8000,  # پورت مورد نظر را اینجا تغییر دهید
    reload=True,
    log_level="info"
)
```

### غیرفعال کردن Auto-Reload
برای محیط production:
```python
uvicorn.run(
    "app.main:app",
    host="0.0.0.0",
    port=8000,
    reload=False,  # غیرفعال شد
    log_level="warning"
)
```

### تغییر Host
برای دسترسی از سایر دستگاه‌های شبکه:
```python
host="0.0.0.0"  # همه آدرس‌ها
```

فقط localhost:
```python
host="127.0.0.1"  # فقط localhost
```

---

## ⚠️ رفع مشکلات رایج

### 1. خطا: "Python is not recognized"
**علت:** Python در PATH سیستم تعریف نشده
**راه‌حل:**
1. Python را مجدداً نصب کنید
2. گزینه "Add Python to PATH" را فعال کنید
3. یا به صورت دستی Python را به PATH اضافه کنید

### 2. خطا: "Access Denied" یا "Permission Error"
**علت:** دسترسی کافی ندارید
**راه‌حل:**
- PowerShell را به عنوان Administrator اجرا کنید
- یا پوشه را به مکان دیگری منتقل کنید (مثلاً `C:\Projects`)

### 3. خطا: "Port 8000 is already in use"
**علت:** پورت 8000 قبلاً استفاده شده
**راه‌حل:**
```powershell
# پیدا کردن process استفاده‌کننده از پورت
netstat -ano | findstr :8000

# متوقف کردن process (PID را از خروجی بالا بگیرید)
taskkill /PID <PID> /F
```

یا پورت را در `run.py` تغییر دهید.

### 4. خطا: "No module named 'app'"
**علت:** در دایرکتوری اشتباه هستید
**راه‌حل:**
```powershell
cd D:\پروژه های دانش بنیان\termo2\termo\server
python run.py
```

### 5. خطا در نصب وابستگی‌ها
**راه‌حل:**
```powershell
# پاک کردن cache pip
pip cache purge

# نصب مجدد
pip install --no-cache-dir -r requirements.txt
```

### 6. خطای "Virtual Environment activation"
**در PowerShell:**
```powershell
# فعال کردن اجرای اسکریپت‌ها
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

## 🧪 تست سرور

### 1. تست با مرورگر
به آدرس زیر بروید:
```
http://127.0.0.1:8000
```
باید پیام خوش‌آمدگویی را ببینید.

### 2. تست API با curl
```powershell
curl http://127.0.0.1:8000
```

### 3. تست API با PowerShell
```powershell
Invoke-WebRequest -Uri http://127.0.0.1:8000 | Select-Object -Expand Content
```

### 4. تست در Swagger UI
1. به `http://127.0.0.1:8000/docs` بروید
2. یک endpoint را انتخاب کنید
3. روی "Try it out" کلیک کنید
4. پارامترها را وارد کنید
5. "Execute" را بزنید

---

## 🔄 به‌روزرسانی سرور

### به‌روزرسانی کد
```powershell
# توقف سرور (Ctrl+C)
# سپس:
git pull origin main

# یا فایل‌های جدید را کپی کنید
```

### به‌روزرسانی وابستگی‌ها
```powershell
# فعال کردن محیط مجازی
.\venv\Scripts\Activate.ps1

# نصب وابستگی‌های جدید
pip install -r requirements.txt --upgrade
```

---

## 🛑 توقف سرور

### روش 1: کلید میانبر
در ترمینالی که سرور در آن اجرا است:
```
Ctrl + C
```

### روش 2: بستن پنجره ترمینال
مستقیماً پنجره را ببندید (توصیه نمی‌شود)

### روش 3: Kill Process
```powershell
# پیدا کردن PID سرور
netstat -ano | findstr :8000

# متوقف کردن
taskkill /PID <PID> /F
```

---

## 📊 مانیتورینگ و Logs

### مشاهده Logs در زمان اجرا
لاگ‌ها به صورت خودکار در ترمینال نمایش داده می‌شوند.

### سطوح Log:
- `INFO`: اطلاعات عمومی
- `WARNING`: هشدارها
- `ERROR`: خطاها
- `DEBUG`: اطلاعات تفصیلی (فقط در حالت توسعه)

### تغییر سطح Log
در `run.py`:
```python
uvicorn.run(
    "app.main:app",
    log_level="debug"  # یا "info", "warning", "error"
)
```

---

## 🔐 امنیت

### نکات امنیتی برای محیط Production:

1. **تغییر CORS Settings:**
   در `app/main.py`:
   ```python
   allow_origins=["http://localhost:5000"]  # فقط دامنه‌های مجاز
   ```

2. **غیرفعال کردن Reload:**
   ```python
   reload=False
   ```

3. **استفاده از HTTPS:**
   - از Nginx یا Apache به عنوان Reverse Proxy استفاده کنید
   - SSL Certificate تهیه کنید

4. **محدود کردن Host:**
   ```python
   host="127.0.0.1"  # فقط localhost
   ```

---

## 📞 دریافت کمک

### منابع:
- مستندات FastAPI: https://fastapi.tiangolo.com/
- مستندات Uvicorn: https://www.uvicorn.org/
- مستندات Python: https://docs.python.org/

### خطایابی:
1. لاگ‌های ترمینال را بررسی کنید
2. به `http://127.0.0.1:8000/docs` بروید
3. از منوی Developer Tools مرورگر استفاده کنید (F12)

---

## ✅ چک‌لیست راه‌اندازی

قبل از شروع کار:
- [ ] Python 3.8+ نصب شده است
- [ ] pip به‌روز شده است
- [ ] محیط مجازی ایجاد و فعال شده است
- [ ] وابستگی‌ها نصب شده‌اند
- [ ] پوشه‌های مورد نیاز ایجاد شده‌اند
- [ ] سرور بدون خطا راه‌اندازی شده است
- [ ] API در مرورگر قابل دسترسی است
- [ ] مستندات Swagger باز می‌شود

---

## 🎯 خلاصه دستورات

```powershell
# راه‌اندازی سریع
cd server
.\start-server.bat

# یا راه‌اندازی دستی
cd server
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
python run.py
```

---

**آخرین به‌روزرسانی:** نوامبر 2025  
**نسخه:** 1.0.0

موفق باشید! 🚀
