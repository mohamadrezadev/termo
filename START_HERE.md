# 🚀 راهنمای سریع راه‌اندازی Termo

## نصب و اجرای کامل پروژه

---

## 📦 پیش‌نیازها

قبل از شروع این موارد را نصب کنید:

1. **Python 3.8+** از [python.org](https://python.org)
   - ✅ حتماً "Add Python to PATH" را تیک بزنید

2. **Node.js LTS** از [nodejs.org](https://nodejs.org)
   - ✅ نسخه LTS را انتخاب کنید

---

## 🎯 روش 1: راه‌اندازی سریع (یک کلیک)

### راه‌اندازی کامل سیستم:
```
دوبار کلیک روی: start-all.bat
```

این فایل **خودکار**:
- ✅ Backend را راه‌اندازی می‌کند (پورت 8000)
- ✅ Frontend را راه‌اندازی می‌کند (پورت 5000)
- ✅ مرورگر را باز می‌کند
- ✅ همه چیز را به صورت خودکار تنظیم می‌کند

---

## 🔧 روش 2: راه‌اندازی جداگانه

### Backend (سرور):
```
cd server
دوبار کلیک: setup-offline.bat
```
آدرس: `http://127.0.0.1:8000`

### Frontend (کلاینت):
```
cd client
دوبار کلیک: start-client.bat
```
آدرس: `http://127.0.0.1:5000`

---

## 📦 نصب آفلاین

### آماده‌سازی Backend:
```
cd server
دوبار کلیک: create-offline-package.bat
```

### آماده‌سازی Frontend:
```
cd client
دوبار کلیک: create-offline-client.bat
```

سپس کل پوشه را به سیستم بدون اینترنت منتقل کنید.

---

## 🌐 دسترسی به سیستم

پس از راه‌اندازی موفق:

| سرویس | آدرس | توضیحات |
|-------|------|---------|
| **کلاینت اصلی** | http://127.0.0.1:5000 | رابط کاربری |
| **API Server** | http://127.0.0.1:8000 | سرور Backend |
| **API Docs** | http://127.0.0.1:8000/docs | مستندات Swagger |
| **ReDoc** | http://127.0.0.1:8000/redoc | مستندات کامل |

---

## 🛑 توقف سرویس‌ها

```
Ctrl + C در هر پنجره ترمینال
```

یا ببندید پنجره‌ها را.

---

## ⚙️ ساختار پروژه

```
termo/
├── start-all.bat              ⭐ راه‌اندازی کامل
│
├── server/                    📡 Backend
│   ├── setup-offline.bat      ← راه‌اندازی سرور
│   └── create-offline-package.bat
│
└── client/                    🖥️  Frontend
    ├── start-client.bat       ← راه‌اندازی کلاینت
    └── create-offline-client.bat
```

---

## 🔧 رفع مشکلات سریع

### ❌ Python یافت نشد
```
دانلود و نصب: https://python.org
```

### ❌ Node.js یافت نشد
```
دانلود و نصب: https://nodejs.org
```

### ❌ پورت اشغال است
```powershell
# Backend (8000)
netstat -ano | findstr :8000
taskkill /PID <شماره> /F

# Frontend (5000)
netstat -ano | findstr :5000
taskkill /PID <شماره> /F
```

### ❌ خطای "execution policy"
```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

## 📊 چک‌لیست راه‌اندازی

- [ ] Python 3.8+ نصب شده
- [ ] Node.js LTS نصب شده
- [ ] فایل `start-all.bat` اجرا شد
- [ ] دو پنجره ترمینال باز شدند
- [ ] مرورگر باز شد و صفحه لود شد
- [ ] ✅ **آماده استفاده!**

---

## 🎯 خلاصه دستورات

```batch
REM راه‌اندازی کامل (پیشنهادی)
start-all.bat

REM یا جداگانه:
cd server && setup-offline.bat
cd client && start-client.bat
```

---

**نسخه:** 1.0.0  
**تاریخ:** نوامبر 2025

موفق باشید! 🚀
